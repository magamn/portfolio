//11․ Տրված է իրական թվերի մատրից։ Արտածել գլխավոր անկյունագծից վերև գտնվող այն դրական տարրերի քանակը, որոնք գտնվում են զույգ համարով սյուներում։

var num = [[1, 2, 3], [4, 30, 40], [5, 9, 6]];

for (var i = 0; i < num.length; ++i) {
    if (num[i] > pow2 && num[i] < p) {
        ++count;
    }
    pow2 = pow2 * 2;
    if (i > 0) {
        p *= i + 1;
    }
}
console.log(count);